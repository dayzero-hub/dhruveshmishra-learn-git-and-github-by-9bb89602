# Operating-system files that are about your machine, not the project
.DS_Store
Thumbs.db

# Editor settings — yours, not the team's
.vscode/
.idea/

# Environment files containing secret API keys and credentials that must never be shared or exposed
.env

# Third-party dependencies that can be reinstalled locally and bloat the repository history
node_modules/
